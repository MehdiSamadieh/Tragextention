#pp:=(x[1]^(2)-2)*(x[1]^(2)-3); 
#qq := z^2 - x[1]^(2);
#R := PolynomialRing([z, x[1]]);

#res := Trager_base(pp, x[1], qq, z, R):
#Display(res, R);

##   [(z * x[1] -2) * (z * x[1] +2),    (x[1]^(2)-2) ]
##   [ (z * x[1] -3) * (z * x[1] +3), (x[1]^(2)-3) ]


##                                                      
#print(" ==================> extened  trager");
        
#T:=[x[1]^(2)-2 ,x[2]^(2)+x[1]-3];
#n:=2;
#qq := z^2 - x[1]^(2);
######################################################################################################################################################################
##############################################################################  extended trager  ##################################################################

print(" extended trager ");

###############################################################################example 1
qq:=(z^2 -2) ;  T:= [(x[1]^2 - 2)*(x[1]^2 - 3)];
n:=1;
R := PolynomialRing([z, x[1]]);
Trager:=ExtendedTrager(qq,T,n,false);
Display(Trager,R);
###############################################################################example 2
#qq:= (z^2 - 3);  T := [x[1]^2 - 2, x[2]^2 - 3]; #qq:= (z^2 - 2)*(z^2 - 3); 
#n:=2;
#R := PolynomialRing([z, x[1], x[2]]);
#Trager:=ExtendedTrager(qq,T,n,false);
#Display(Trager,R);
 #####################################################################################example 3
#qq:= z^2 - 6;   T := [x[1]^2 - 2,x[2]^2 - 3];
#n:=2;
#R := PolynomialRing([z, x[1], x[2]]);
#Trager:=ExtendedTrager(qq,T,n,false);
#Display(Trager,R);
#Trager:=ExtendedTrager(qq,T,n,true);
################################################################################example 4 
#qq:=  (z^2  -  x[1]) ;
#T := [(x[1]^2 - 2)*( x[1]^2 - 3), x[2]^2  -x[1] ];
#R := PolynomialRing([z, x[1], x[2]]);
#n:=2;
###############################################################################simple example

#Trager:=ExtendedTrager(qq,T,n,false);
#Display(Trager,R);
#Trager:=ExtendedTrager(qq,T,n,true);


